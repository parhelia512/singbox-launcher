# Чеклист перед публикацией на GitHub

## ✅ Критично - Проверьте перед коммитом!

### 🔒 Файлы с секретами (НЕ коммитить!)

- [ ] **`bin/config.json`** - содержит реальные токены, UUID прокси, секреты Clash API
  - ✅ Создан `bin/config.json.example` (без секретов) - этот файл можно коммитить
  - ⚠️ Убедитесь, что `bin/config.json` в `.gitignore`

### 📦 Бинарники и артефакты сборки

- [ ] `singbox-launcher.exe` - скомпилированный файл (уже в .gitignore)
- [ ] `rsrc.syso` - артефакт сборки иконки (уже в .gitignore)
- [ ] `bin/sing-box.exe` - бинарник sing-box (уже в .gitignore)
- [ ] `bin/wintun.dll` - библиотека WinTun (уже в .gitignore)
- [ ] `bin/parser.exe` - парсер (уже в .gitignore)

### 📝 Логи и временные файлы

- [ ] `logs/*.log` - все логи (уже в .gitignore)
- [ ] `compile_debug.txt` - отладочный файл (уже в .gitignore)
- [ ] `releases/` - папка с релизами (уже в .gitignore)
- [ ] `fyne-cross/` - временная папка кросс-компиляции (уже в .gitignore)

### 📋 Что ДОЛЖНО быть в репозитории

- ✅ Исходный код (`.go` файлы)
- ✅ `go.mod`, `go.sum`
- ✅ Документация (`README.md`, `BUILD_WINDOWS.md`, `ParserConfig.md`, `CHANGELOG.md`)
- ✅ Build скрипты (`build/`)
- ✅ Assets (иконки в `assets/`)
- ✅ `.gitignore`
- ✅ `bin/config.json.example` - пример конфигурации БЕЗ секретов
- ✅ `LICENSE` (если есть)

## 🔍 Проверка перед коммитом

Выполните в PowerShell:

```powershell
# Проверить, что config.json не будет закоммичен
git check-ignore bin/config.json

# Должно вывести: bin/config.json

# Проверить статус файлов
git status

# Убедитесь, что НЕ видите:
# - bin/config.json
# - singbox-launcher.exe
# - rsrc.syso
# - logs/*.log
# - releases/
```

## ⚠️ Если bin/config.json уже был закоммичен

Если вы случайно закоммитили `bin/config.json` с секретами:

1. **Немедленно** измените все секреты (токены, UUID, пароли)
2. Удалите файл из истории Git:
   ```bash
   git rm --cached bin/config.json
   git commit -m "Remove config.json with secrets"
   ```
3. Если уже запушили в публичный репозиторий - **смените все секреты немедленно!**

## 📝 Рекомендации

1. **Перед первым коммитом:**
   ```bash
   git status
   ```
   Проверьте список файлов - не должно быть секретов и бинарников

2. **Создайте .gitattributes** (опционально):
   ```
   bin/config.json linguist-generated=true
   ```

3. **Добавьте в README предупреждение:**
   - Укажите, что `bin/config.json` нужно создать из `config.json.example`
   - Напомните пользователям не коммитить свои конфиги

## ✅ Готово к публикации

После проверки всех пунктов можно делать:
```bash
git add .
git commit -m "Initial commit: Cross-platform sing-box launcher"
git push origin main
```


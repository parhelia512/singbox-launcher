# Отчет об инспекции кода проекта singbox-launcher

**Дата:** 21 декабря 2025  
**Версия:** После рефакторинга config packages

## Резюме

Проект находится в рабочем состоянии, код компилируется и функционирует. После успешного рефакторинга структуры config пакетов код стал более организованным, однако выявлены архитектурные проблемы, требующие внимания для дальнейшего развития проекта.

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (High Priority)

### 1. God Object: AppController содержит слишком много ответственности

**Проблема:**
- `AppController` содержит **152 поля** (core/controller.go:58-152)
- **7+ различных мьютексов** для синхронизации различных частей состояния
- Нарушает принцип Single Responsibility Principle (SRP)
- Файл `controller.go` - **1197 строк**

**Последствия:**
- Сложность поддержки и тестирования
- Высокий риск race conditions
- Сложность добавления новых функций
- Проблемы с пониманием кода новыми разработчиками

**Предложения:**
```
ПРИОРИТЕТ: ВЫСОКИЙ
1. Разбить AppController на отдельные сервисы:
   - UIService (UI state, callbacks, tray menu)
   - APIService (Clash API interactions, proxy list)
   - StateService (running state, version cache, auto-update state)
   - FileService (log files, config paths)

2. Вынести конфигурацию в отдельную структуру:
   - AppConfig struct с полями путей и конфигурации
   
3. Использовать dependency injection вместо хранения всех зависимостей в одном месте
```

### 2. Огромный файл config_wizard.go

**Проблема:**
- `ui/config_wizard.go` - **2284 строки кода**
- **120+ функций** в одном файле
- Смешивает UI логику, бизнес-логику и утилиты

**Последствия:**
- Сложность навигации и поиска кода
- Проблемы с merge конфликтами
- Высокая когнитивная нагрузка

**Предложения:**
```
ПРИОРИТЕТ: ВЫСОКИЙ
1. Разбить на модули:
   - wizard/preview.go - превью узлов
   - wizard/outbound_editor.go - редактор outbounds
   - wizard/source_editor.go - редактор источников
   - wizard/template_loader.go - загрузка шаблонов
   - wizard/validation.go - валидация данных
   - wizard/helpers.go - вспомогательные функции

2. Вынести бизнес-логику в отдельные сервисы
3. Создать отдельные компоненты для повторяющихся UI паттернов
```

### 3. Множественные мьютексы в AppController

**Проблема:**
Найдено минимум 7 различных мьютексов:
- `CmdMutex` - для процесса sing-box
- `ParserMutex` - для парсера конфига
- `APIStateMutex` - для API состояния
- `AutoLoadMutex` - для авто-загрузки
- `TrayMenuUpdateMutex` - для обновления трея
- `VersionCheckMutex` - для кеша версий
- `LauncherVersionCheckMutex` - для версии лаунчера
- `AutoUpdateMutex` - для авто-обновлений

**Последствия:**
- Высокий риск deadlock при сложных взаимодействиях
- Сложность отладки race conditions
- Потенциальные проблемы производительности

**Предложения:**
```
ПРИОРИТЕТ: ВЫСОКИЙ
1. Инкапсулировать связанные поля и мьютексы в отдельные структуры:
   type APIService struct {
       mutex sync.RWMutex
       proxiesList []api.ProxyInfo
       activeProxyName string
       ...
   }
   
2. Использовать каналы для асинхронной коммуникации вместо shared state
3. Документировать порядок захвата мьютексов для предотвращения deadlock
```

## 🟡 ВАЖНЫЕ ПРОБЛЕМЫ (Medium Priority)

### 4. Смешанные языки в комментариях

**Проблема:**
- Комментарии частично на русском, частично на английском
- Имена функций на английском, но комментарии часто на русском
- Затрудняет работу международных контрибьюторов

**Примеры:**
```go
// Обрабатываем результат
if err != nil {
    // Update progress: Step 1 completed
    updateParserProgress(ac, 5, "Parsed ParserConfig block")
}
```

**Предложения:**
```
ПРИОРИТЕТ: СРЕДНИЙ
1. Унифицировать язык комментариев (рекомендуется английский)
2. Перевести все русские комментарии на английский
3. Добавить в CI проверку единообразия комментариев
```

### 5. Отсутствие валидации входных данных

**Проблема:**
- Недостаточная валидация URL подписок
- Отсутствие проверки размеров данных перед обработкой
- Потенциальные проблемы с очень большими конфигурациями

**Примеры проблемных мест:**
- `subscription/fetcher.go` - нет ограничения размера ответа HTTP
- `config/parser/factory.go` - нет валидации размера JSON
- `subscription/node_parser.go` - нет проверки размера URI

**Предложения:**
```
ПРИОРИТЕТ: СРЕДНИЙ
1. Добавить лимиты на размер HTTP ответов (например, 10MB)
2. Валидировать размеры JSON конфигураций
3. Добавить таймауты для всех сетевых операций
4. Использовать context.WithTimeout для всех операций с сетью
```

### 6. Недостаточное покрытие тестами

**Проблема:**
- Основные тесты есть только для subscription/node_parser
- Нет интеграционных тестов для критичных путей
- Удален `config_service_impl_test.go` без замены

**Текущее покрытие:**
- `core/config/subscription/node_parser_test.go` - хорошее покрытие парсинга
- `core/integration_test.go` - базовые интеграционные тесты
- `ui/config_wizard_test.go` - минимальное покрытие

**Предложения:**
```
ПРИОРИТЕТ: СРЕДНИЙ
1. Добавить тесты для:
   - config/parser/factory.go (ExtractParserConfig, NormalizeParserConfig)
   - config/updater.go (UpdateConfigFromSubscriptions)
   - config/generator.go (GenerateSelector, GenerateNodeJSON)
   
2. Интеграционные тесты:
   - Полный цикл обновления конфига из подписки
   - Обработка ошибок сети и парсинга
   
3. Добавить table-driven tests для граничных случаев
```

### 7. Потенциальные утечки ресурсов

**Проблема:**
- Файловые дескрипторы могут не закрываться при ошибках
- HTTP клиенты могут не закрываться правильно
- Goroutines могут завершаться некорректно

**Примеры:**
```go
// core/controller.go - log files
logFile, err := openLogFileWithRotation(...)
// Нет гарантии закрытия при панике

// subscription/fetcher.go - HTTP requests
resp, err := http.Get(url)
// Нужно проверить закрытие Body
```

**Предложения:**
```
ПРИОРИТЕТ: СРЕДНИЙ
1. Всегда использовать defer для закрытия ресурсов:
   defer resp.Body.Close()
   defer file.Close()
   
2. Добавить context для отмены долгих операций
3. Использовать sync.WaitGroup для goroutines
4. Добавить graceful shutdown для всех фоновых задач
```

### 8. Обработка ошибок

**Проблема:**
- Непоследовательная обработка ошибок
- Иногда ошибки логируются, но не возвращаются
- Отсутствие structured logging

**Примеры:**
```go
// Иногда только логируем
log.Printf("Error: %v", err)

// Иногда возвращаем
return fmt.Errorf("failed: %w", err)

// Иногда игнорируем
_ = someFunction()
```

**Предложения:**
```
ПРИОРИТЕТ: СРЕДНИЙ
1. Унифицировать обработку ошибок:
   - Всегда возвращать ошибки из функций
   - Логировать на уровне выше с контекстом
   
2. Использовать structured logging (log/slog или zap)
3. Добавить error wrapping для контекста
4. Использовать errors.Is и errors.As для проверки типов ошибок
```

## 🟢 УЛУЧШЕНИЯ (Low Priority)

### 9. Дублирование кода

**Проблема:**
- Похожие функции для работы с конфигом в разных местах
- Дублирование логики валидации

**Примеры:**
- Проверка существования файлов повторяется в нескольких местах
- Похожая логика обработки JSONC

**Предложения:**
```
ПРИОРИТЕТ: НИЗКИЙ
1. Вынести общие утилиты в отдельные функции
2. Создать shared utilities package
3. Использовать код-генерацию для повторяющихся паттернов
```

### 10. Отсутствие документации API

**Проблема:**
- Недостаточно godoc комментариев
- Нет примеров использования для публичных API
- Отсутствие архитектурной документации

**Предложения:**
```
ПРИОРИТЕТ: НИЗКИЙ
1. Добавить godoc комментарии для всех экспортируемых функций
2. Создать примеры использования (examples/)
3. Обновить README с описанием архитектуры
4. Добавить диаграммы зависимостей пакетов
```

### 11. Константы и магические числа

**Проблема:**
- Магические числа в коде (таймауты, лимиты)
- Некоторые константы определены в одном месте, другие разбросаны

**Примеры:**
```go
time.Sleep(2 * time.Second) // Магическое число
delay = 100 * time.Millisecond // Почему 100?
```

**Предложения:**
```
ПРИОРИТЕТ: НИЗКИЙ
1. Вынести все магические числа в именованные константы
2. Группировать константы по функциональности
3. Документировать причину выбора значений
```

### 12. TODO/FIXME комментарии

**Проблема:**
- Найдено 18 файлов с TODO/FIXME комментариями
- Некоторые могут указывать на технический долг

**Предложения:**
```
ПРИОРИТЕТ: НИЗКИЙ
1. Провести аудит всех TODO/FIXME
2. Создать issues для важных TODO
3. Удалить устаревшие комментарии
4. Добавить дату и автора для важных TODO
```

## 📊 Метрики кода

### Размеры файлов:
- `core/controller.go`: 1197 строк (требует рефакторинга)
- `ui/config_wizard.go`: 2284 строки (критично)
- `core/process_service.go`: ~457 строк (приемлемо)
- `core/config_service.go`: 131 строк (хорошо после рефакторинга)

### Структура пакетов:
- ✅ `core/config/` - хорошо организован после рефакторинга
- ✅ `core/config/parser/` - четкая ответственность
- ✅ `core/config/subscription/` - хорошее разделение
- ⚠️ `core/` - слишком много ответственности в controller.go
- ⚠️ `ui/` - config_wizard.go слишком большой

### Качество:
- ✅ Нет циклических зависимостей
- ✅ Четкое разделение пакетов
- ⚠️ Много мьютексов (потенциальные проблемы)
- ⚠️ Смешанные языки комментариев

## 🎯 Рекомендуемый план действий

### Фаза 1: Критические проблемы (1-2 месяца)
1. **Рефакторинг AppController** - разбить на сервисы
2. **Разбить config_wizard.go** - разделить на модули
3. **Упростить мьютексы** - инкапсулировать состояние

### Фаза 2: Важные улучшения (2-3 месяца)
4. Унифицировать комментарии
5. Добавить валидацию входных данных
6. Улучшить покрытие тестами
7. Исправить потенциальные утечки ресурсов

### Фаза 3: Полировка (по мере возможности)
8. Унифицировать обработку ошибок
9. Устранить дублирование кода
10. Добавить документацию API
11. Вынести константы

## ✅ Положительные моменты

1. **Успешный рефакторинг config packages** - чистая архитектура
2. **Хорошая структура подпакетов** - нет циклических зависимостей
3. **Использование defer** - правильная работа с ресурсами
4. **Обработка ошибок** - большинство ошибок обрабатываются
5. **Тесты для парсера узлов** - хорошее покрытие критичной логики

## 📝 Заключение

Проект находится в рабочем состоянии, но требует архитектурного рефакторинга для обеспечения долгосрочной поддерживаемости. Критические проблемы связаны с размером и сложностью `AppController` и `config_wizard.go`. После решения этих проблем проект будет готов к дальнейшему развитию.

**Общая оценка:** 6.5/10
- Функциональность: 8/10
- Архитектура: 5/10
- Качество кода: 7/10
- Тестирование: 6/10
- Документация: 6/10


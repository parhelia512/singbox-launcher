# План рефакторинга singbox-launcher

**Основано на:** `CODE_INSPECTION_REPORT.md` и ответах на `REFACTORING_QUESTIONS.md`

## 📋 Принципы работы

### ✅ Разрешено:
- Ломать внутренний API между пакетами
- Менять структуру кода, названия функций
- Менять логику реализации (но не функционал)
- Удалять и переписывать старые тесты
- Улучшать производительность
- Уменьшать количество кода
- Разбивать большие файлы на модули

### ❌ Запрещено:
- Менять структуру конфигов (формат config.json, ParserConfig)
- Терять функциональность
- Менять UI/UX (внешний вид и поведение интерфейса)
- Ухудшать производительность

### 📝 Требования:
- Код самодокументируемый (хорошие названия, структура)
- Комментарии на английском, объясняют ЗАЧЕМ (не что/как)
- Критическая бизнес-логика покрыта функциональными тестами
- Тесты пишутся после рефакторинга
- Стратегия: постепенный рефакторинг (ориентируясь на размер контекста)

---

## 🎯 Фаза 1: Рефакторинг AppController (ПРИОРИТЕТ: КРИТИЧЕСКИЙ)

### Цель
Разбить God Object на отдельные сервисы, уменьшить количество мьютексов, улучшить читаемость.

### Задачи

#### 1.1. Создать структуру сервисов
```
core/
├── services/
│   ├── ui_service.go          # UI state, callbacks, tray menu
│   ├── api_service.go         # Clash API, proxy list management
│   ├── state_service.go       # Running state, version cache
│   ├── file_service.go        # Log files, config paths
│   └── cache_service.go       # Version caches, timers
```

**Действия:**
- Вынести UI-related поля из AppController в `UIService`
- Вынести API-related поля в `APIService`
- Вынести state management в `StateService`
- Вынести file operations в `FileService`
- Вынести caching логику в `CacheService`

**Метрики успеха:**
- AppController < 30 полей (сейчас 152)
- Количество мьютексов уменьшено (инкапсулированы в сервисы)
- controller.go < 500 строк

#### 1.2. Инкапсулировать мьютексы
- Каждый сервис управляет своим мьютексом
- Документировать порядок захвата мьютексов (если нужен)
- Использовать каналы для асинхронной коммуникации где возможно

#### 1.3. Dependency Injection
- Сервисы получают зависимости через конструкторы
- AppController координирует сервисы, но не хранит все состояние

### Ожидаемый результат
- AppController стал координатором, а не хранилищем состояния
- Код проще тестировать (можно мокировать сервисы)
- Меньше риск race conditions
- Легче добавлять новые функции

---

## 🎯 Фаза 2: Рефакторинг config_wizard.go (ПРИОРИТЕТ: ВЫСОКИЙ)

### Цель
Разбить огромный файл (2284 строки) на логические модули, сохранив функциональность и UI.

### Задачи

#### 2.1. Разбить на модули
```
ui/wizard/
├── wizard.go                  # Main wizard entry point
├── preview.go                 # Node preview logic
├── outbound_editor.go        # Outbound editing
├── source_editor.go          # Source editing
├── template_handler.go       # Template loading/management
├── validation.go             # Input validation
├── helpers.go                # Utility functions
└── components.go             # Reusable UI components
```

**Действия:**
- Вынести preview логику в `preview.go`
- Вынести outbound editing в `outbound_editor.go`
- Вынести source editing в `source_editor.go`
- Вынести template handling в `template_handler.go`
- Вынести validation в `validation.go`
- Вынести утилиты в `helpers.go`
- Создать переиспользуемые UI компоненты в `components.go`

**Метрики успеха:**
- Каждый файл < 300 строк
- Нет дублирования кода между модулями
- UI/UX не изменился (функционально идентичен)

#### 2.2. Вынести бизнес-логику
- Переместить бизнес-логику из UI в `core/config/` или новые сервисы
- UI только отображает и собирает данные

---

## 🎯 Фаза 3: Улучшение качества кода (ПРИОРИТЕТ: СРЕДНИЙ)

### Задачи

#### 3.1. Валидация и безопасность
- Добавить лимиты на размер HTTP ответов (10MB)
- Добавить таймауты для всех сетевых операций
- Валидировать размеры JSON конфигураций
- Использовать `context.WithTimeout` для долгих операций

**Файлы для изменения:**
- `core/config/subscription/fetcher.go` - HTTP requests
- `core/config/parser/factory.go` - JSON parsing
- `core/config/subscription/node_parser.go` - URI parsing

#### 3.2. Обработка ошибок
- Унифицировать обработку ошибок (всегда возвращать, логировать на уровне выше)
- Использовать error wrapping (`fmt.Errorf("...: %w", err)`)
- Использовать `errors.Is` и `errors.As` для проверки типов

#### 3.3. Утечки ресурсов
- Проверить все `defer Close()` для файлов и HTTP responses
- Добавить context для отмены долгих операций
- Использовать `sync.WaitGroup` для goroutines

#### 3.4. Унификация комментариев
- Перевести все русские комментарии на английский
- Комментарии объясняют ЗАЧЕМ, не что/как
- Удалить избыточные комментарии (код самодокументируемый)

#### 3.5. Константы
- Вынести все магические числа в именованные константы
- Группировать константы по функциональности
- Документировать причину выбора значений

---

## 🎯 Фаза 4: Тестирование (ПРИОРИТЕТ: СРЕДНИЙ)

### Задачи

#### 4.1. Функциональные тесты для критической логики
- `core/config/parser/factory.go` - ExtractParserConfig, NormalizeParserConfig
- `core/config/updater.go` - UpdateConfigFromSubscriptions
- `core/config/generator.go` - GenerateSelector, GenerateNodeJSON
- `core/config/subscription/source_loader.go` - LoadNodesFromSource
- `core/config/subscription/node_parser.go` - ParseNode (улучшить существующие)

**Требования к тестам:**
- Функциональные (проверяют поведение, не реализацию)
- Покрывают граничные случаи
- Table-driven tests где уместно
- Можно удалить старые тривиальные тесты

#### 4.2. Интеграционные тесты
- Полный цикл обновления конфига из подписки
- Обработка ошибок сети и парсинга
- Миграция версий конфигов

---

## 🎯 Фаза 5: Документация и финализация (ПРИОРИТЕТ: НИЗКИЙ)

### Задачи

#### 5.1. Godoc комментарии
- Добавить godoc для всех экспортируемых функций
- Примеры использования для публичного API

#### 5.2. Архитектурная документация
- Обновить README с описанием новой архитектуры
- Диаграммы зависимостей пакетов (опционально)

---

## 📊 Метрики успеха (итоговые)

### Количественные:
- ✅ AppController < 30 полей (было 152)
- ✅ controller.go < 500 строк (было 1197)
- ✅ config_wizard.go разбит на модули < 300 строк каждый (было 2284)
- ✅ Количество мьютексов уменьшено (инкапсулированы в сервисы)
- ✅ Общее количество кода уменьшено
- ✅ Покрытие тестами критической логики > 80%

### Качественные:
- ✅ Функционал сохранен (все работает как раньше)
- ✅ Код разбит понятнее (легко найти нужную логику)
- ✅ Код читаемее (самодокументируемый, хорошие названия)
- ✅ UI/UX не изменился
- ✅ Производительность не ухудшилась (лучше если возможно)
- ✅ Структура конфигов не изменилась

---

## 🚀 План выполнения

### Итерация 1: AppController - UIService
**Цель:** Вынести UI-related поля и логику
- Создать `core/services/ui_service.go`
- Переместить UI state, callbacks, tray menu логику
- Обновить AppController для использования UIService
- Проверить что все работает

### Итерация 2: AppController - APIService
**Цель:** Вынести API-related поля и логику
- Создать `core/services/api_service.go`
- Переместить Clash API interactions, proxy list management
- Обновить AppController
- Проверить что все работает

### Итерация 3: AppController - StateService
**Цель:** Вынести state management
- Создать `core/services/state_service.go`
- Переместить running state, version cache, auto-update state
- Обновить AppController
- Проверить что все работает

### Итерация 4: AppController - FileService и CacheService
**Цель:** Вынести file operations и caching
- Создать `core/services/file_service.go` и `cache_service.go`
- Переместить соответствующие поля
- Финальная очистка AppController
- Проверить что все работает

### Итерация 5: config_wizard.go - Preview и Helpers
**Цель:** Начать разбиение config_wizard.go
- Вынести preview логику в `ui/wizard/preview.go`
- Вынести утилиты в `ui/wizard/helpers.go`
- Проверить что UI работает идентично

### Итерация 6: config_wizard.go - Editors
**Цель:** Вынести редакторы
- Вынести outbound editing в `ui/wizard/outbound_editor.go`
- Вынести source editing в `ui/wizard/source_editor.go`
- Проверить что UI работает идентично

### Итерация 7: config_wizard.go - Template и Validation
**Цель:** Завершить разбиение
- Вынести template handling в `ui/wizard/template_handler.go`
- Вынести validation в `ui/wizard/validation.go`
- Создать переиспользуемые компоненты в `ui/wizard/components.go`
- Проверить что UI работает идентично

### Итерация 8: Валидация и безопасность
**Цель:** Улучшить безопасность
- Добавить лимиты и таймауты
- Улучшить обработку ошибок
- Проверить утечки ресурсов

### Итерация 9: Тестирование
**Цель:** Покрыть критическую логику тестами
- Написать функциональные тесты для parser, updater, generator
- Написать интеграционные тесты
- Удалить устаревшие тривиальные тесты

### Итерация 10: Документация и финализация
**Цель:** Завершить рефакторинг
- Унифицировать комментарии
- Добавить godoc
- Финальная проверка метрик

---

## 📝 Примечания

1. **Постепенный подход:** Каждая итерация должна оставлять проект в рабочем состоянии
2. **Тестирование после:** Тесты пишутся после рефакторинга каждого модуля
3. **Ориентация на контекст:** Размер изменений зависит от контекста - можно делать большие шаги если уверены
4. **Функциональность прежде всего:** Главное - сохранить весь функционал
5. **Самодокументируемый код:** Хорошие названия важнее комментариев

---

## ✅ Готовность к началу

Все вопросы решены, план готов. Можно начинать с Фазы 1, Итерации 1.



# Промпт для AI агента: Рефакторинг singbox-launcher

## Контекст задачи

Ты должен провести комплексный рефакторинг проекта `singbox-launcher` по двум направлениям одновременно:

1. **Рефакторинг AppController и структуры core/** - согласно `todo/REFACTORING_PLAN.md`
2. **Рефакторинг UI визарда конфигурации** - согласно `todo/REFACTOR_UI_WIZARD_STRUCTURE.md`

Оба направления должны быть выполнены параллельно, где это возможно, с учетом зависимостей между ними.

## Исходные документы

**Обязательно изучить перед началом:**
- `todo/CODE_INSPECTION_REPORT.md` - отчет об инспекции кода с выявленными проблемами
- `todo/REFACTORING_PLAN.md` - общий план рефакторинга (AppController, улучшения качества)
- `todo/REFACTOR_UI_WIZARD_STRUCTURE.md` - детальный план рефакторинга UI визарда

**Дополнительно:**
- `todo/complete/REFACTOR_CONFIG_STRUCTURE.md` - уже выполненный рефакторинг config пакетов (для понимания стиля)

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Использование команд

**Используй ТОЛЬКО неинтерактивные команды, которые НЕ требуют пользовательского ввода или разрешения.**

### ✅ Разрешенные команды (безопасные):

**Встроенные функции инструментов (предпочтительно):**
- `read_file`, `write`, `search_replace`, `delete_file` - работа с файлами
- `grep`, `codebase_search` - поиск в коде
- `list_dir`, `glob_file_search` - работа с файловой системой
- `read_lints` - проверка ошибок

**Go команды (только чтение/проверка, неинтерактивные):**
- `go build ./core/... ./ui/...` - компиляция (без флагов, требующих подтверждения)
- `go test ./... -count=1` - тесты (с `-count=1` для избежания кеширования)
- `go vet ./...` - статический анализ
- `go list ./...` - список пакетов
- `go fmt ./...` - форматирование

### ❌ ЗАПРЕЩЕНО (требуют разрешения):

- Git операции: `git commit`, `git push`, `git pull`, `git merge`, `git rebase`
- Интерактивные команды: любые с флагом `-i` (interactive)
- Редакторы: `vim`, `nano`, `notepad`, любые команды, открывающие редактор
- Скрипты сборки: `.\build\build_windows.bat` (может требовать подтверждения)
- Команды с подтверждением: `rm -i`, `cp -i`, и т.п.

**Правило:** Если команда может остановиться и ждать ввода - НЕ используй её. Всегда используй встроенные функции инструментов когда это возможно.

## Принципы работы

### ✅ Разрешено:
- Ломать внутренний API между пакетами (core/, ui/)
- Менять структуру кода, названия функций и переменных
- Менять логику реализации (но не функционал)
- Удалять и переписывать старые тесты
- Улучшать производительность
- Уменьшать общее количество кода
- Разбивать большие файлы на модули
- Создавать новые подпакеты и структуры

### ❌ Запрещено:
- Менять структуру конфигов (формат config.json, ParserConfig структура)
- Терять функциональность (все должно работать как раньше)
- Менять UI/UX (внешний вид и поведение интерфейса должны остаться идентичными)
- Ухудшать производительность (можно только улучшать)
- Делать Git операции (commit, branch, push, pull)

### 📝 Требования:
- **Самодокументируемый код**: хорошие названия функций и переменных важнее комментариев
- **Комментарии на английском**: объясняют ЗАЧЕМ (не что/как делается)
- **Тесты после рефакторинга**: пиши тесты после завершения рефакторинга модуля, не до
- **Функциональные тесты**: тесты проверяют поведение, не реализацию
- **Постепенный подход**: каждая итерация оставляет проект в рабочем состоянии
- **Ориентация на размер контекста**: делай изменения того размера, который можешь держать в контексте

## План работы (20 итераций)

**ВАЖНО:** План является **примерным и гибким**. Ты можешь адаптировать количество итераций под реальный размер изменений, объединять небольшие итерации или разбивать большие на более мелкие шаги. Главное - сохранить логическую структуру фаз и обеспечить работоспособность проекта после каждого значимого шага.

**Рекомендации по адаптации:**
- Если изменения небольшие - можно объединить несколько итераций
- Если изменения большие - разбей на под-итерации для контроля
- Всегда ориентируйся на размер контекста - делай изменения, которые можешь держать в голове
- После каждой фазы (группы связанных итераций) обязательно пиши промежуточный отчет

### Фаза 1: AppController рефакторинг (Итерации 1-4)

#### Итерация 1: AppController - UIService
**Цель:** Вынести UI-related поля и логику
- Создать `core/services/ui_service.go`
- Переместить UI state, callbacks, tray menu логику из `AppController`
- Обновить `AppController` для использования `UIService`
- Обновить все места использования UI-related полей
- **Проверка:** проект компилируется и запускается

#### Итерация 2: AppController - APIService
**Цель:** Вынести API-related поля и логику
- Создать `core/services/api_service.go`
- Переместить Clash API interactions, proxy list management
- Инкапсулировать `APIStateMutex` в `APIService`
- Обновить `AppController` и все места использования
- **Проверка:** проект компилируется и запускается

#### Итерация 3: AppController - StateService
**Цель:** Вынести state management
- Создать `core/services/state_service.go`
- Переместить running state, version cache, auto-update state
- Инкапсулировать соответствующие мьютексы
- Обновить `AppController` и все места использования
- **Проверка:** проект компилируется и запускается

#### Итерация 4: AppController - FileService и CacheService
**Цель:** Вынести file operations и caching
- Создать `core/services/file_service.go` (log files, config paths)
- Создать `core/services/cache_service.go` (version caches, timers)
- Переместить соответствующие поля и мьютексы
- Финальная очистка `AppController` (должно остаться < 30 полей)
- **Проверка:** проект компилируется, `controller.go` < 500 строк

### Фаза 2: UI Wizard структура - State и Utils (Итерации 5-6)

#### Итерация 5: Wizard State структура
**Цель:** Создать структуру состояния визарда
- Создать `ui/wizard/state/state.go` (переместить из `config_wizard_state.go`)
- Создать `ui/wizard/state/helpers.go` (safeFyneDo, логирование)
- Создать `ui/wizard/utils/constants.go` (вынести все магические числа)
- Обновить импорты в существующем коде
- **Проверка:** проект компилируется

#### Итерация 6: Wizard Utils
**Цель:** Создать утилиты визарда
- Создать `ui/wizard/utils/outbound.go` (утилиты для outbounds)
- Создать `ui/wizard/utils/preview.go` (утилиты для превью)
- Создать `ui/wizard/utils/navigation.go` (навигация)
- Создать `ui/wizard/utils/comparison.go` (сравнение структур)
- Переместить соответствующие функции из `config_wizard.go`
- **Проверка:** проект компилируется

### Фаза 3: UI Wizard - Template и Dialogs (Итерации 7-8)

#### Итерация 7: Wizard Template
**Цель:** Вынести работу с шаблонами
- Создать `ui/wizard/template/loader.go` (из `config_template_loader.go`)
- Создать `ui/wizard/template/processor.go` (обработка шаблонов)
- Создать `ui/wizard/template/formatter.go` (форматирование)
- Переместить функции из `config_wizard.go`
- Обновить импорты
- **Проверка:** проект компилируется, UI работает идентично

#### Итерация 8: Wizard Dialogs
**Цель:** Вынести диалоги
- Создать `ui/wizard/dialogs/rule_dialog.go` (из `config_wizard_rule_dialog.go`)
- Переместить функции диалогов из `config_wizard.go`
- Обновить импорты
- **Проверка:** проект компилируется, диалоги работают

### Фаза 4: UI Wizard - Business Logic (Итерации 9-12)

#### Итерация 9: Wizard Business - Parser
**Цель:** Вынести парсинг и обработку
- Создать `ui/wizard/business/parser.go`
- Переместить `parseAndPreview()`, `checkURL()`, `applyURLToParserConfig()`
- **Добавить валидацию:** лимиты размера HTTP ответов (10MB), таймауты, валидация URI
- **Обработка ошибок:** все ошибки возвращаются, используется error wrapping
- **Ресурсы:** defer resp.Body.Close(), context.WithTimeout
- Обновить импорты
- **Проверка:** проект компилируется, парсинг работает

#### Итерация 10: Wizard Business - Generator
**Цель:** Вынести генерацию конфига
- Создать `ui/wizard/business/generator.go`
- Переместить `buildTemplateConfig()`, `buildParserOutboundsBlock()`, `mergeRouteSection()`
- Обновить импорты
- **Проверка:** проект компилируется, генерация работает

#### Итерация 11: Wizard Business - Validator
**Цель:** Вынести валидацию
- Создать `ui/wizard/business/validator.go`
- Реализовать валидацию ParserConfig, URL, правил
- Добавить проверки размеров данных (JSON, URI, HTTP responses)
- Использовать константы из `utils/constants.go`
- Обновить импорты
- **Проверка:** проект компилируется, валидация работает

#### Итерация 12: Wizard Business - Loader
**Цель:** Вынести загрузку и сохранение
- Создать `ui/wizard/business/loader.go`
- Переместить `loadConfigFromFile()`, `serializeParserConfig()`, `ensureRequiredOutbounds()`
- **Добавить валидацию:** проверка размера файлов
- **Ресурсы:** defer file.Close() для всех файловых операций
- Обновить импорты
- **Проверка:** проект компилируется, загрузка/сохранение работает

### Фаза 5: UI Wizard - Tabs (Итерации 13-15)

#### Итерация 13: Wizard Tabs - Source Tab
**Цель:** Вынести UI первой вкладки
- Создать `ui/wizard/tabs/source_tab.go`
- Переместить `createVLESSSourceTab()` из `config_wizard.go`
- Использовать функции из `business/`, `utils/`, `state/`
- Обновить импорты
- **Проверка:** проект компилируется, UI идентичен

#### Итерация 14: Wizard Tabs - Rules Tab
**Цель:** Вынести UI вкладки правил
- Создать `ui/wizard/tabs/rules_tab.go`
- Переместить `createTemplateTab()`, `createRulesScroll()`
- Использовать `dialogs/` для диалогов правил
- Обновить импорты
- **Проверка:** проект компилируется, UI идентичен

#### Итерация 15: Wizard Tabs - Preview Tab
**Цель:** Вынести UI вкладки превью
- Создать `ui/wizard/tabs/preview_tab.go`
- Переместить `createPreviewTab()`
- Использовать функции из `business/generator.go`
- Обновить импорты
- **Проверка:** проект компилируется, UI идентичен

### Фаза 6: UI Wizard - Entry Point и Cleanup (Итерации 16-17)

#### Итерация 16: Wizard Entry Point
**Цель:** Создать точку входа визарда
- Создать `ui/wizard/wizard.go`
- Переместить `ShowConfigWizard()` и логику инициализации
- Координировать все подпакеты (tabs, business, state, etc.)
- Обновить все импорты в `ui/` для использования `ui/wizard`
- **Проверка:** проект компилируется, визард открывается

#### Итерация 17: Удаление старых файлов
**Цель:** Очистить старую структуру
- Удалить `ui/config_wizard.go` (содержимое распределено)
- Удалить `ui/config_wizard_state.go` (в `wizard/state/`)
- Удалить `ui/config_wizard_rule_dialog.go` (в `wizard/dialogs/`)
- Удалить `ui/config_template_loader.go` (в `wizard/template/`)
- Проверить что нет ссылок на старые файлы
- **Проверка:** проект компилируется, все работает

### Фаза 7: Улучшение качества и унификация (Итерации 18-19)

#### Итерация 18: Валидация и безопасность в core/
**Цель:** Улучшить безопасность в core пакетах
- Добавить лимиты на размер HTTP ответов в `core/config/subscription/fetcher.go`
- Добавить таймауты для всех сетевых операций (context.WithTimeout)
- Валидировать размеры JSON в `core/config/parser/factory.go`
- Валидировать размеры URI в `core/config/subscription/node_parser.go`
- Проверить все defer Close() для ресурсов
- **Проверка:** проект компилируется, все работает

#### Итерация 19: Унификация комментариев и ошибок
**Цель:** Улучшить качество кода
- Перевести все русские комментарии на английский (объясняют ЗАЧЕМ)
- Унифицировать обработку ошибок (error wrapping, возврат ошибок)
- Вынести оставшиеся магические числа в константы
- Удалить избыточные комментарии (код самодокументируемый)
- **Проверка:** проект компилируется, код читаемее

### Фаза 8: Тестирование и финализация (Итерация 20)

#### Итерация 20: Тестирование критической логики
**Цель:** Покрыть тестами критическую бизнес-логику
- Написать функциональные тесты для `wizard/business/parser.go`
- Написать функциональные тесты для `wizard/business/generator.go`
- Написать функциональные тесты для `wizard/business/validator.go`
- Написать функциональные тесты для `wizard/business/loader.go`
- Написать интеграционные тесты для полного цикла визарда
- Улучшить существующие тесты в `core/config/` (удалить тривиальные)
- **Проверка:** все тесты проходят, покрытие критической логики > 80%

## Метрики успеха

### Количественные проверки:
- ✅ `AppController` < 30 полей (было 152)
- ✅ `controller.go` < 500 строк (было 1197)
- ✅ `config_wizard.go` удален, заменен модулями < 300 строк каждый
- ✅ Количество мьютексов уменьшено (инкапсулированы в сервисы)
- ✅ Общее количество кода уменьшено
- ✅ Все файлы компилируются без ошибок
- ✅ Все тесты проходят

### Качественные проверки:
- ✅ Функционал полностью сохранен (все работает как раньше)
- ✅ UI/UX идентичен (нет визуальных изменений)
- ✅ Код разбит понятнее (легко найти нужную логику)
- ✅ Код читаемее (самодокументируемый, хорошие названия)
- ✅ Производительность не ухудшилась (лучше если возможно)
- ✅ Структура конфигов не изменилась
- ✅ Нет циклических зависимостей

## Правила работы

1. **После каждой итерации:**
   - Проект должен компилироваться (используй `go build` для проверки)
   - Критичный функционал должен работать (если возможно проверить)
   - Обновлять TODO список по мере выполнения

2. **Приоритеты:**
   - Сохранение функциональности > красота кода
   - Рабочий код > идеальная архитектура
   - Постепенные изменения > большие переписывания

3. **Комментарии:**
   - Пиши комментарии только когда нужно объяснить ЗАЧЕМ
   - Код должен быть самодокументируемым через названия
   - Все комментарии на английском языке

4. **Ошибки:**
   - Всегда возвращай ошибки из функций
   - Используй `fmt.Errorf("...: %w", err)` для wrapping
   - Логируй ошибки на уровне выше (в вызывающем коде)

5. **Ресурсы:**
   - Всегда используй `defer Close()` для файлов и HTTP responses
   - Используй `context.WithTimeout` для долгих операций
   - Проверяй размеры данных перед обработкой

6. **Команды терминала:**
   - Используй ТОЛЬКО неинтерактивные команды
   - Если сомневаешься - используй встроенные функции инструментов вместо команд терминала

## Промежуточные отчеты

**После завершения каждой фазы** (группы связанных итераций) пиши промежуточный отчет на русском языке.

**Когда писать промежуточные отчеты:**
- После Фазы 1 (итерации 1-4) - рефакторинг AppController
- После Фазы 2 (итерации 5-6) - Wizard State и Utils
- После Фазы 3 (итерации 7-8) - Wizard Template и Dialogs
- После Фазы 4 (итерации 9-12) - Wizard Business Logic
- После Фазы 5 (итерации 13-15) - Wizard Tabs
- После Фазы 6 (итерации 16-17) - Entry Point и Cleanup
- После Фазы 7 (итерации 18-19) - Улучшение качества

**Структура промежуточного отчета:**
1. **Название фазы** (например, "Фаза 1: Рефакторинг AppController")
2. **Выполненные итерации** (какие итерации были сделаны)
3. **Что изменено:**
   - Созданные файлы/пакеты
   - Измененные файлы
   - Удаленные файлы
   - Ключевые архитектурные решения
4. **Метрики:**
   - Размеры файлов (до/после)
   - Количество полей/функций
   - Строки кода (если применимо)
5. **Проверки:**
   - Компилируется ли проект
   - Работает ли функционал
   - Пройдены ли тесты
6. **Проблемы и решения** (если были)
7. **Следующие шаги** (что планируется в следующей фазе)

**Где сохранять:** Создай файл `todo/REFACTORING_PROGRESS.md` и дополняй его после каждой фазы (или можешь создавать отдельные файлы `todo/REFACTORING_PHASE_1.md`, `todo/REFACTORING_PHASE_2.md` и т.д.)

**Важно:** Промежуточные отчеты помогают отслеживать прогресс и выявлять проблемы на раннем этапе. Пиши их кратко, но информативно.

---

## Итоговый отчет

После завершения всех итераций (всех фаз) напиши финальный подробный отчет на русском языке со следующей структурой:

### Структура отчета:

1. **Резюме**
   - Что было сделано
   - Основные изменения
   - Метрики (до/после)

2. **Детальное описание изменений**
   - По каждой фазе: что было изменено, какие файлы созданы/удалены
   - Архитектурные решения и их обоснование

3. **Метрики успеха**
   - Количественные показатели (размеры файлов, количество полей, строк кода)
   - Качественные улучшения (читаемость, структура)

4. **Проблемы и решения**
   - С какими проблемами столкнулся
   - Как они были решены
   - Компромиссы (если были)

5. **Проверка функциональности**
   - Что было проверено
   - Какие тесты добавлены/удалены
   - Результаты проверок

6. **Рекомендации на будущее**
   - Что можно улучшить дальше
   - Потенциальные проблемы
   - Следующие шаги

Отчет сохрани в файл `todo/REFACTORING_REPORT.md`.

**Связь с промежуточными отчетами:** Итоговый отчет должен резюмировать все промежуточные отчеты, но с акцентом на общую картину и финальные метрики.

## Начало работы

Начни с изучения всех исходных документов, затем приступай к Итерации 1. Работай последовательно, проверяя после каждой итерации что проект компилируется.

**Удачи!**


# Changelog

## [Unreleased] - Рефакторинг для кроссплатформенности

### Добавлено
- ✅ Кроссплатформенная архитектура с поддержкой Windows, macOS и Linux
- ✅ Пакет `internal/platform` для платформо-специфичного кода
- ✅ Build скрипты для разных платформ (`build/build_windows.bat`, `build/build_darwin.sh`, `build/build_linux.sh`)
- ✅ Подробный README.md с инструкциями по установке и сборке
- ✅ Функция `GetSelectorGroupsFromConfig` для извлечения селекторов из конфигурации

### Изменено
- 🔄 Рефакторинг `core/controller.go` для использования платформо-специфичных функций
- 🔄 Рефакторинг `ui/main_ui.go` для использования платформо-специфичных функций
- 🔄 Вынесены Windows-специфичные команды (`taskkill`, `explorer`) в `internal/platform`
- 🔄 Унифицированы пути к исполняемым файлам через платформо-специфичные функции

### Структура проекта
```
singbox-launcher/
├── api/                    # Clash API клиент
├── assets/                 # Иконки и ресурсы
├── bin/                    # Исполняемые файлы и конфигурация
├── build/                  # Скрипты сборки
│   ├── build_windows.bat
│   ├── build_darwin.sh
│   └── build_linux.sh
├── cmd/                    # Точки входа приложения
│   └── desktop/           # Desktop версия (зарезервировано)
├── core/                   # Основная логика приложения
│   ├── config_parser.go   # Парсинг конфигурации
│   └── controller.go      # Основной контроллер
├── internal/              # Внутренние пакеты
│   └── platform/         # Платформо-специфичный код
│       ├── platform_windows.go
│       ├── platform_darwin.go
│       └── platform_common.go
├── ui/                    # Пользовательский интерфейс
│   ├── clash_api_ui.go
│   └── main_ui.go
├── main.go                # Точка входа
├── README.md              # Документация
└── go.mod                 # Зависимости
```

### Платформо-специфичные функции

Все платформо-специфичные операции вынесены в пакет `internal/platform`:

- `GetExecutableNames()` - имена исполняемых файлов
- `GetWintunPath()` - путь к wintun.dll (только Windows)
- `OpenFolder()` - открытие папки в файловом менеджере
- `OpenURL()` - открытие URL в браузере
- `KillProcess()` - завершение процесса по имени
- `KillProcessByPID()` - завершение процесса по PID
- `PrepareCommand()` - подготовка команды с платформо-специфичными атрибутами
- `GetRequiredFiles()` - список необходимых файлов
- `GetProcessNameForCheck()` - имя процесса для проверки

### Следующие шаги

- [ ] Тестирование на macOS
- [ ] Тестирование на Linux
- [ ] Поддержка Android
- [ ] CI/CD для автоматической сборки всех платформ

